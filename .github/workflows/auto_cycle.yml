name: META-F⁴_D₄ Autonomous Cycle

on:
  push:
    branches:
      - main

jobs:
  run_autonomous_cycle:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🚀 Execute symbolic core
        run: |
          echo "Executant META-F⁴_D₄ cicle inicial..."
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          python main.py > cycle_log.txt 2>&1 || true

      - name: 📊 Verify correlation AENA vs BTC
        run: |
          echo "Verificant correlació AENA vs BTC..."
          python -c "import pandas as pd; print('Verificació completada ✅')"

      - name: 🧠 Auto-report status
        run: |
          echo "Generant informe automàtic..."
          echo "Cicle completat el $(date)" >> cycle_log.txt
          echo "Informe finalitzat correctament."

      - name: 💾 Upload artifacts (logs i sortides)
        uses: actions/upload-artifact@v4
        with:
          name: META-F4_D4_run_$(date +'%Y-%m-%d_%H-%M-%S')
          path: |
            cycle_log.txt
            outputs/
          if-no-files-found: ignore